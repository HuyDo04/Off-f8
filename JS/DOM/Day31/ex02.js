const print = (n) => {
    if (n > 0) {
      print(n - 1);
    }
    console.log(n);
  };
  
  print(10);

//   Bước 2: duyệt qua từng phần tử của mảng, thêm li, a, href, textnode
// Bước 3: Kiểm tra parentId:
/**
 * 
 */